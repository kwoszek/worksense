import DefaultLayout from "@/layouts/default";

export default function AboutPage() {
  return (
    <DefaultLayout>
      <section className="max-w-2xl mx-auto p-6">
        <h1 className="text-2xl font-semibold mb-2">About Focus</h1>
        {/* Content intentionally left empty for you to fill in. */}
      </section>
    </DefaultLayout>
  );
}
